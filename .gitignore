# Generated by build.bat / build.sh
int/
universal-analog-plugin/
universal-analog-plugin-with-wooting-device-support/

# Build folders, manually zipped
*.zip

# Unnamed .sun file for Ctrl+B can be used-defined
.sun

# Compiled plugin outputs (dylib = macOS, so = Linux)
# Note: wooting_analog_common.a and .lib are source files and are NOT ignored.
*.dylib
*.so
lib*.o

# macOS spike harness binaries
macos-spike/read_test
macos-spike/kcprobe
